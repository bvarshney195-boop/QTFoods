# Optional build-time configuration.
# Leave unset when the frontend is served by Laravel (including Render) so all
# requests use the same-origin /api path. Set only for a deliberate split-origin deployment.
VITE_API_BASE_URL=
